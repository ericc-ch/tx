# Docs

Short reference for workspace conventions.

| Doc                                      | Topic                                        |
| ---------------------------------------- | -------------------------------------------- |
| [typecheck.md](typecheck.md)             | Root `tsc -b` solution and what gets checked |
| [package-exports.md](package-exports.md) | Cross-package imports without a build step   |

Run `bun run typecheck` from the repo root (also part of `bun run check`).
